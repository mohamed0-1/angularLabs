import { CreditInfoPipe } from './credit-info.pipe';

describe('CreditInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
