import { PersonalInfoPipe } from './personal-info.pipe';

describe('PersonalInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
